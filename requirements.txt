# TouchDiffusion MKII — dependencies
# Install PyTorch with CUDA first (match your CUDA version):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# Core (SD.py, gradio_prepare.py, StreamDiffusion)
numpy==2.4.2
PyYAML==6.0.3
gradio==5.50.0
gradio_toggle==2.0.2
groovy==0.1.2

# StreamDiffusion (install_requires from setup.py)
diffusers @ git+https://github.com/varshith15/diffusers.git@3e3b72f557e91546894340edabc845e894f00922
transformers==4.56.0
accelerate==1.10.0
huggingface_hub==0.35.0
Pillow==11.0.0
fire==0.6.0
omegaconf==2.3.0

# TRT engine build (gradio_prepare: DepthAnything, YOLO, Flux Klein)
opencv-python==4.13.0.92
ultralytics>=8.0.0
onnx==1.18.0
onnxscript==0.6.2
onnxruntime==1.23.2
onnxruntime-gpu==1.23.2
polygraphy==0.49.24
protobuf==4.25.3
onnx-graphsurgeon==0.5.8
colored==2.2.4
# TensorRT (StreamDiffusion, Depth Anything, YOLO, Flux Klein). From NVIDIA index; CUDA 12 below.
# For CUDA 11 run from repo root (venv + StreamDiffusion on path): python -m streamdiffusion.tools.install-tensorrt --cu 11
--extra-index-url https://pypi.nvidia.com
tensorrt==10.12.0.36

# ControlNet (if enabled in config)
controlnet-aux==0.0.10

# IP-Adapter: vendored in repo (diffusers_ipadapter/), added via sys.path; no pip install.

# Windows
pywin32==306; sys_platform == 'win32'
